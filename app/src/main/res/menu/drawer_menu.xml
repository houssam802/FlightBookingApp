<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    >
    <group android:checkableBehavior="single">
        <item android:id="@+id/fl_booking"
            android:icon="@drawable/ic_flight"
            android:title="Réservation de vol"
            ></item>
        <item android:id="@+id/fl_booked"
            android:icon="@drawable/ic_booking"
            android:title="Vol réserver"
            ></item>

        <item android:id="@+id/profile"
            android:icon="@drawable/ic_account_circle"
            android:title="Profile"
            ></item>

    </group>
    <item android:title="@null">
        <menu>
            <item android:id="@+id/logout"
                android:icon="@drawable/ic_logout"
                android:title="Log out"
                ></item>
        </menu>
    </item>
</menu>